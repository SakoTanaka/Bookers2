<p id="notice"><%= flash[:notice] %> </p>
<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Opinipn</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<tbody>
  　<% @book.each do |book| %>
   　<tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to 'Show', book_path(book) %></td>
</tr>
    <% end %>
  </tbody>
</table>